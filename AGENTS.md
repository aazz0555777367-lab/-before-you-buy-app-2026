# دليل التطبيق التقني وخطة الصيانة | Technical Documentation & Maintenance Plan

تطبيق **"قبل تشتري | Before You Buy"** هو مساعد شراء ذكي مبني على بنية برمجية متكاملة (Full-Stack) باستخدام تقنيات حديثة تدمج بين الذكاء الاصطناعي الفائق وتجربة المستخدم السلسة والممتازة الشبيهة بتطبيقات شركة Apple.

---

## 1. البنية التقنية (Tech Stack & Architecture)

- **واجهة المستخدم (Frontend):** React 19 + TypeScript + Vite + Tailwind CSS v4.
- **الخادم الخلفي (Backend Node.js):** Express Server مدمج مع Vite middleware في وضع التطوير لخدمة أحادية الصفحة (SPA).
- **محرك الذكاء الاصطناعي:** SDK الرسمي الحديث `@google/genai` (موديل `gemini-3.5-flash`).
- **الحركية والحركة البصرية:** `motion` للتنقلات السلسة والرسوميات الجذابة.
- **تتبع البيانات والأمنيات:** محركات تخزين محلية موحدة (Durable Local Storage Sync) قابلة للمزامنة السحابية الفورية.

---

## 2. هيكلية المجلدات والملفات (Directory Structure)

```
/
├── server.ts               # الخادم الخلفي (Express) ومحرك معالجة Gemini API
├── package.json            # التبعيات والسكربتات الخاصة بالبناء والتشغيل
├── index.html              # ملف الإدخال الأساسي للويب
├── metadata.json           # بيانات التطبيق وصلاحيات الإطار (مثل الكاميرا)
├── src/
│   ├── main.tsx            # ملف ربط React ونقطة الانطلاق
│   ├── index.css           # ملف التنسيق الشامل واستيراد خطوط IBM Plex Sans Arabic
│   ├── App.tsx             # المحرك الرئيسي وإدارة شاشات التطبيق الـ 15
│   ├── types.ts            # النماذج والأنماط الموحدة لمطابقة معايير جودة الكود
│   ├── translations.ts     # قاموس الترجمة الموحد (عربي / إنجليزي)
│   ├── data/
│   │   └── mockProducts.ts # قاعدة بيانات مرجعية سريعة للمنتجات الأكثر رواجاً للتجربة الفورية
│   └── components/
│       ├── Splash.tsx             # شاشة الترحيب الفاخرة (Apple Style)
│       ├── Onboarding.tsx         # تجميع بيانات ملف المشتري (طالب، ألعاب، عمل، تصوير)
│       ├── Login.tsx              # شاشة تخصيص التجربة والاسم
│       ├── HomeView.tsx           # اللوحة الرئيسية والبحث ومفاتيح تشغيل الكاميرا
│       ├── ProductDetailsView.tsx # التقرير الشامل، درجة التقييم، البدائل، وتاريخ الأسعار
│       ├── ComparisonView.tsx     # المقارنة الذكية وجهاً لوجه بين منتجين
│       ├── SmartWishlist.tsx      # قائمة الأمنيات الذكية ومفاتيح تتبع الكوبونات والتوفر
│       ├── HistoryView.tsx        # سجل الفحص والمسح التاريخي السابق
│       ├── NotificationsView.tsx  # الإشعارات والخصومات الفورية
│       └── SettingsView.tsx       # تعديل تفضيلات المشتري، المظهر، واللغة
```

---

## 3. محرك ذكاء تحليل المنتجات (Smart Analysis Engine)

يتعامل الخادم في `server.ts` مع طلبات الفحص من خلال خطوتين ذكيتين تضمنان بقاء التطبيق حياً ومستقراً حتى في حال عدم توفر مفتاح برمجيات خارجي:
1. **التعرف البصري والباركود:** يقبل الخادم مسوحات صورية حقيقية (base64) عبر الكاميرا والباركود ويقوم Gemini بالتعرف الفوري على الموديل.
2. **البحث المترابط (Search Grounding):** يدمج التطبيق أداة `googleSearch` للبحث الحي في الأسواق العربية والخليجية لجمع الأسعار والكوبونات الفورية لضمان دقة لا تضاهى.
3. **التراجع المرن (Graceful Fallbacks):** في حال وجود أي عائق تقني أو غياب للمفتاح، يتراجع التطبيق لإنتاج تقرير تحليلي عالي الجودة والدقة بشكل محاكى بدلاً من تعطل واجهة المستخدم.

---

## 4. خطة الصيانة والتوسع المستمر (Maintenance & Scalability Plan)

### المرحلة الأولى: مراقبة الأداء واستقرار الـ API
- **تحديثات الـ API ومفاتيح الربط:** مراقبة حصة استهلاك مفاتيح Gemini عبر كونسول Google AI Studio، والترقية للموديلات الأحدث فور صدورها.
- **تحديث المراجعات المزيفة:** ضبط خوارزميات الذكاء الاصطناعي وتحسين هندسة الأوامر (Prompt Engineering) بشكل دوري لضمان كفاءة كشف التقييمات الترويجية والمدفوعة عبر المتاجر.

### المرحلة الثانية: جاهزية النشر على متاجر الهواتف (Native Deployment)
بما أن التطبيق مصمم ومبني كـ **PWA (Progressive Web App)** متكامل وسلس، فهو جاهز تماماً للتحويل إلى تطبيق أصيل (Native Android/iOS) باستخدام أحد الأساليب التالية:
1. **CapacitorJS / Cordova:** تغليف كود الـ React الحالي مباشرةً لإنتاج كود Swift و Kotlin متكامل يدعم الكاميرا والإشعارات الأصلية.
2. **Flutter WebView:** استخدام إطار Flutter كغلاف فاخر يدير الكاميرا والتنبيهات، ويشير مباشرةً إلى عنوان التطبيق على السحاب لضمان تحديثات فورية بدون الحاجة لإرسال تحديثات للمتجر في كل مرة.

### المرحلة الثالثة: الميزات المتقدمة للتحديث القادم
- **ربط API مباشر للمتاجر:** دمج واجهات ربط المتاجر الكبرى (أمازون، نون) لقراءة لحظية للأسعار والمخزون دون الاعتماد الكلي على زحف الويب.
- **إشعارات نظام التشغيل الفورية (Push Notifications):** ربط خادم التطبيق بـ Firebase Cloud Messaging (FCM) لإرسال التنبيهات حتى في حال إغلاق التطبيق.
